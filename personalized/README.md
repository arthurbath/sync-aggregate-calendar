# Personalized Scripts

Put private, configured copies of the scripts from `src/` in this directory before running the compiler scripts.

The compiler scripts build from `personalized/`, not from `src/`. Files added here are ignored by git except for this README and the directory's `.gitignore`.
